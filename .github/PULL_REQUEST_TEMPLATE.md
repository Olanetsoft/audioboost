**What this changes**

**How it was tested**
- [ ] `python3 -m unittest discover tests` passes
- [ ] Tried against a real recording (for audio-path changes, include a
      before/after `loudnorm` measurement)
